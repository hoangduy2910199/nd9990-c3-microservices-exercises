apiVersion: v1
kind: Service
metadata:
  name: udagram-frontend
  namespace: c3-project
spec:
  selector:
    app.kubernetes.io/name: udagram-frontend
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080